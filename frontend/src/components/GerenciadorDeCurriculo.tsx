import WorkInProgress from "./WorkInProgress";

const GerenciadorDeCurriculo = () => {
  return (
    <div className="flex justify-center items-center h-screen w-screen bg-deep-gray text-light">
      <WorkInProgress />
    </div>
  );
};

export default GerenciadorDeCurriculo;
