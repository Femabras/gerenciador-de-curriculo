name: Deploy React App to GitHub Pages

on:
  # Trigger the workflow on pushes to the main branch
  push:
    branches: ["main"]

# Grant necessary permissions for the deployment actions
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    # CRITICAL: This sets the default context for 'run:' commands (npm ci, npm run build)
    # to the 'frontend' sub-folder
    defaults:
      run:
        working-directory: ./frontend

    steps:
      # 1. Checkout the repository code
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Setup Node.js environment
      # Removed 'cache' and 'cache-dependency-path' to bypass the nested path error.
      # Dependencies will be installed in Step 3.
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # 3. Install dependencies - runs inside 'frontend' folder
      - name: Install dependencies
        run: npm ci

      # 4. Build the React application - runs inside 'frontend' folder
      - name: Build
        run: npm run build

      # 5. Upload the 'dist' folder content as a deployment artifact
      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Path to the build output, relative to the job root.
          path: "./frontend/dist"

      # 6. Deploy the uploaded artifact to GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions:deploy-pages@v4
